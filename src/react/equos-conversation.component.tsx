import { CreateEquosBrowserSessionResponse } from '../core/types/session.types';

export default function EquosConversation({
  session,
}: {
  session: CreateEquosBrowserSessionResponse;
}) {
  return <div>EquosConversation Component</div>;
}
